// Template for local secrets. Copy to `Secrets.xcconfig` (gitignored) and fill in,
// then set it as the project's base configuration in Xcode (see
// docs/YOUTUBE_DATA_API_KEY.md). Leaving the key empty makes the made-for-kids
// gate fail closed (no videos can be added) — safe, but Song Review will reject
// every add until a real key is provided.

// YouTube Data API v3 key for the made-for-kids check (Spec §14.1).
YOUTUBE_DATA_API_KEY =
